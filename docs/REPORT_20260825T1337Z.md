# 汇报: 全量 runs tar 核 sha 通过 — **pod 可杀**

- 时间 (UTC): 2026-08-25T13:37Z
- git HEAD: `781b172` (observation; this report is the next commit)
- 协议: protocol_v2 / pv2 (未改)
- 数据包: tv5
- runs: 完整归档 `runs/harvest/forge_runs_full.tar.gz`（含 adapters 与 `_isolated/`）

## 归档

| 项 | 值 |
|---|---|
| 路径 | `runs/harvest/forge_runs_full.tar.gz` |
| 大小 | 8.2 GB |
| sha256 | `71309f7c73eee3f4bc0c9781ef5655d560ae976b5ee00a86110a8aebf4028f7d` |
| pod 一致 | 是（`FULLTAR_OK` 13:21:48Z；scp+本地 sha 13:37Z） |
| 旧档 | `/tmp/forge_runs.tar.gz` 404MB (2026-08-22) **不是**本轮产物 |

增量 incr 1–7 仍在 `runs/harvest/`（无 adapters）。incr 7 sha `e9363b62…e11679`。

## Wave-1 摘要（不改）

tydiqa / task419 S6 ok（oom_fallback）。bird PARTIAL S2 OOM。apps PARTIAL S4 OOM（8h 帽已让 S1 完成）。

## 操作员

**pod 可杀。** agent 不杀。杀之前不要清 `/tmp/forge_runs_full.tar.gz` 以外的东西也无妨：本机 sha 已对齐。

`--final` 分析在本归档核验之后另交。
