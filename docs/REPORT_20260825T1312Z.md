# 汇报: Wave-1 四槽结束; apps S4 OOM PARTIAL; harvest incr 7; tmux rerun 退出

- 时间 (UTC): 2026-08-25T13:12Z
- git HEAD: `db628d0` (observation; this report is the next commit)
- 协议: protocol_v2 / pv2 (yaml 未改)
- 数据包: tv5 (`latest_tasks_ver`); bird dual-sha checker 已绿
- S4 声明: `2fb2db2`
- runs: 见下; 失败目录仍在 `runs/_isolated/`（未删）

## Wave-1 结果

| task | dir | status | note |
|---|---|---|---|
| bird | `bird__pv2__tv5__20260825-0620` | PARTIAL | S0 dual-sha 绿; S1 0.085/0.23; **S2 CUDA OOM**; 无 metrics; 不在 oom_fallback 白名单 |
| tydiqa | `tydiqa__pv2__tv5__20260825-0703` | ok | oom_fallback=true; Δ_full **+0.530** |
| task419 | `…__tv5__20260825-0836` | ok | oom_fallback=true; Δ_full **+0.505** |
| apps | `apps__pv2__tv5__20260825-0929` | PARTIAL | **8h 帽生效** (S1 跑完, 非 over_budget); S1 pass1=0.0 pass8=0.005; Δ_pilot 0; **S4 step 22/495 CUDA OOM** (tried 4.07 GiB, 611 MiB free / 44.43 GiB); 无 metrics.json |

apps 日志: tokenizer 警告 `289242 > 131072`（随后仍按 max_seq_len 4096 截断）。8h 护栏把 S1 测完了, 全量训练仍 OOM。未把 apps 加入 oom_fallback, 未改协议, 未 Mini-Dev, 未重启。

tmux `rerun` **gone** (名单 4/4 跑完, 预期退出)。GPU 空闲。

## harvest incr 7

scp `runs/harvest/forge_incr_7.tar.gz` sha256 `e9363b6223eeadcd037ebe885c6aabde01b4a7c67ece8935d8a1a6bcc1e11679` 与 pod 一致。1.5MB, **无 adapters**。4 个 tv5 目录: bird, tydiqa, task419, apps。

## 下一步 (Wave 2)

完整 `runs/` tar（含 adapters + `_isolated/`）尚未打。本报告之后开始。打完并核 sha 才写「pod 可杀」。`--final` 分析仍等 Wave 2。
