# 汇报: pv2 冻结 · 信号回填 · 分析预注册

- 时间 (UTC): 2026-08-22T06:58Z
- git HEAD: `a9d28fd` (`docs/ANALYSIS_PREREG.md` 入库)
- 协议: protocol_v2 / pv2(冻结; 无 protocol_v3; yaml 未改)
- 数据包: tasks_v3 / tv3
- runs:
  - `runs/gsm8k__pv2__tv3__20260821-1038/`
  - `runs/winogrande__pv2__tv3__20260821-2105/`
  - `runs/spider__pv2__tv3__20260821-2137/`

## 裁决 1

`protocol_v2` 为量产协议。pilot 10×100 原样保留。三个冒烟 run 算量产数据: **3/46, 剩 43**。未启动剩余任务,仓内仍只有 gsm8k / winogrande / spider。

## 裁决 2

`src/signals.py` 从既有日志派生 `metrics.signals`。未改 `train_lora.py` / `eval_greedy.py` / 协议。未来 S2 把 `train step` 行落到 `pilot_loss.jsonl`; S6 写入 `signals`。三封 run 已本地回填(`scripts/backfill_signals.py`)。台账一行,不升版本。`runs/` gitignore,数字不进 git。

| task | Δ_pilot | headroom | loss start→end | steps_to_0.01 | train_n / full_n | gen_len median chars (base/pilot/full) |
|---|---|---|---|---|---|---|
| gsm8k | −0.335 | 0.20 | 0.449→3.67e-05 | 15 | 7473 / 7473 | 822.5 / 210 / 211 |
| winogrande | −0.055 | 0.18 | 0.733→2.41e-05 | 6 | 8000 / 8000 | 1 / 1 / 1 |
| spider | −0.155 | 0.175 | 0.482→9.52e-05 | 19 | 8659 / 8000 | 616.5 / 118.5 / 81.5 |

生成长度单位是 **chars**(jsonl `output`),不是 token。WinoGrande 为 1 是因为只答字母。

## 裁决 3

`docs/ANALYSIS_PREREG.md` 已在 `a9d28fd` 入库,早于任何剩余量产 run。主终点 go ⇔ Δ_full > 0; 副终点 Spearman(Δ_pilot→Δ_full); logistic + 单特征基线; 任务级 LOTO; 任务级 bootstrap AUC CI; 纯任务描述对照(禁用 `prior_label`)。诚实条款原文: AUC 不显著 = 负结果照发。未实现 sklearn、未跑分析。

## 验证

`python -m pytest tests/ -q` → 62 passed。无 `protocol_v3.yaml`。停点: 剩余 43 的任务名单不在仓内,未打包、未开跑。
