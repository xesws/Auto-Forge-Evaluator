# 汇报: 单 pod 串行发车准备

- 时间 (UTC): 2026-08-22T08:25Z
- git HEAD: `a147a4a` (local; push with ship)
- 协议: protocol_v2 / pv2
- 数据包: **tasks_v4** sha256 `3021a45c78ee182ff2610a83121573ef3f1accae67e5d92a0eaf2f84c8a4c8b0` (56MB, 59 包, 无 bird)
- runs: 尚未开 GPU; 增量回收目录 `runs/harvest/`

## 名单

`docs/prod_lists/prod_serial.txt`: 9 文献 → 50 SuperNI → bird 队尾。pod B 取消。

## 闸门

`run_pod_list.py`: 3h 帽, 缺包 skip, 第 1/10/20… 全量 200 复跑否则 30 条 seed 20260820, 每 10 个任务 `/tmp/forge_incr_k.tar.gz`(不含 adapters)。Mac 拉到 `runs/harvest/`。

## BIRD

并行物化, 2h 不绿则排除句: excluded for logistics, before any bird number existed。成功则 `tasks_v5` 仅 bird, 不改 v4。

## 验证

`python -m pytest tests/ -q` → 135 passed。无 protocol_v3。
