# Part 3 material: reflection candidates

Operator writes the 1-page Broader Impact from these bullets.
Do not put usernames, repo names, or hostnames in the PDF.

1. **False-negative connectivity.** A tool layer refused SSH and the agent reported "cannot connect" while the mux was live. Autonomous long jobs need a connectivity check that cannot be confused with policy refusal.
2. **Schema lag vs logistics.** BIRD died on `KeyError: sha256` because the checker wanted a field the package did not have. That is not "the model cannot learn SQL." Closing a queue with an invented label is forbidden; closing with the logistics sentence is allowed only before any label exists.
3. **Format overwrite vs EOS.** GSM8K Δ_full −0.230 (ANALYSIS_RESULTS.md L58) is format saturation plus reasoning collapse, not an EOS bug. Negative transfer on a heavily post-trained base is a real no-go mode.
4. **Wall as ops fence.** APPS 3h then 8h: the fence is operational, not a measurement. The run still produced no Δ_full. Base 0.0 / pass@8 0.005 is a missing-negative footnote, not a row.
5. **Negative-class scarcity.** 8 no-go / 61 (L24) is why the pre-registered binary gate has no power. Future go/no-go studies should budget for hard negatives, not only for n.
6. **Description features reverse.** Text-only AUC 0.309 (L17, L44). Skipping the GPU and reading dataset size is not a substitute for the probe.
7. **Process rule worth copying.** Pre-register the honesty clause before remaining runs; freeze the protocol file; do not reopen logistics excludes after seeing labels.
