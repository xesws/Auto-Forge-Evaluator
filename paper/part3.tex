\section{Broader Impact}
\label{sec:part3}
% CFP: $\leq$ 1 page. Narrative is operator-owned.
% No Part~3 prose is written here.

\begin{center}\texttt{[OPERATOR PROSE HERE]}\end{center}

\subsubsection*{Reflection-candidate list}
\label{sec:part3-materials}
% Candidates only. Operator writes the 1-page reflection.
\begin{enumerate}
  \item Tool-layer false-negative connectivity vs.\ actual host failure: a process risk for autonomous long jobs.
  \item Schema lag (checker vs.\ dual zip-sha fields) vs.\ logistics: labels must not be invented to close a queue.
  \item Format overwrite vs.\ EOS as the GSM8K mechanism
    ($\Delta_{\mathrm{full}}=-0.230$).
    % AR.md L58
  \item Wall-clock as ops fence vs.\ measurement semantics (APPS 3h then 8h, still no $\Delta_{\mathrm{full}}$).
    % AR.md L30
  \item Negative-class scarcity ($8$ no-go / $61$) as a first-class design constraint for any go/no-go endpoint.
    % AR.md L24
  \item Description-only features running below chance (AUC $0.309$): skipping the GPU is not a cheap substitute.
    % AR.md L17, L44
  \item How the field should adapt: pre-register the honesty clause before the remaining runs; freeze the protocol; do not reopen logistics excludes after seeing labels.
\end{enumerate}
